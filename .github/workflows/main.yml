name: make release

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: push to release repo
      run: |
          git remote set-url origin https://github.com/josesoaresif/optest.git
          git commit -m "Built files"
          git push origin master
